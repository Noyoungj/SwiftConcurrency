# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwiftCurrency' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftCurrency
  pod 'SnapKit'
  pod 'Then'
  target 'SwiftCurrencyTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SwiftCurrencyUITests' do
    # Pods for testing
  end

end
